name: Validate Documentation
on: [push, pull_request]
jobs:
  lint-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint documentation
        run: python tools/lint_docs_layout.py
