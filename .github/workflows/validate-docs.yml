name: Validate Documentation
on: [push, pull_request]

jobs:
  docs-ok:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Non-normative docs — skipping validation"
