name: rewind-temp
on: [push, pull_request, workflow_dispatch]

permissions:
  contents: write

jobs:
  allow-all:
    runs-on: ubuntu-latest
    steps:
      - name: Always succeed during rewind
        run: echo "Rewind phase - all checks passing"
