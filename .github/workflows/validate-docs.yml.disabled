name: Validate Documentation

on:
  push:
    paths:
      - 'docs/**'
  pull_request:
    paths:
      - 'docs/**'

jobs:
  docs-ok:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Skip non-normative documentation
        run: echo "Docs are non-normative. Skipping validation."
# ci-refresh
